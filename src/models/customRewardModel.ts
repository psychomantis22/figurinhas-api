export type customRewardType = {
    id: string;
    title: string;
    prompt: string;
};

let customRewardModel: customRewardType = {
    id: '',
    title: '',
    prompt: ''
};

export default customRewardModel;